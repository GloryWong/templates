{
  "name": "{{it.project.name}}",
  "type": "module",
  "version": "0.0.0",
  "description": "",
  "author": {
    "name": "{{it.user.name}}",
    "email": "{{it.user.email}}",
    "url": "{{it.user.url}}"
  },
  "license": "MIT",
  "homepage": "https://github.com/{{it.user.name}}/{{it.project.name}}#readme",
  "repository": "github:{{it.user.name}}/{{it.project.name}}",
  "bugs": "https://github.com/{{it.user.name}}/{{it.project.name}}/issues",
  "keywords": [],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
